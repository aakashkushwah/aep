apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acctmgmt-service.name" . }}
  labels:
    app: {{ include "acctmgmt-service.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "acctmgmt-service.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "acctmgmt-service.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8092

